import { IdentificacionModel } from './identificacion.model';

describe('IdentificacionModel', () => {
  it('should create an instance', () => {
    expect(new IdentificacionModel()).toBeTruthy();
  });
});
